#include "Calculation.h"


int main() {
	//можно мен€ть, что именно подаетс€ в функцию, убира€ "//" перед вызовом функции или мен€€ значени€.
	double time = T / 13.6;
	double perigee_time = 0;
	double apogee_time = T / 2.0;
	//calculation(time);
	//calculation(perigee_time);
	//calculation(apogee_time);
	//for (double time_for = 0.0; time_for < T; time_for++) {
	//	calculation(time_for);
	//}
	return 0;
}